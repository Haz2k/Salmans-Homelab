---
all:
  children:
    proxmox_nodes:
      hosts:
        pve1:
          ansible_host: 192.168.1.11   # TODO: set real IP
        pve2:
          ansible_host: 192.168.1.12   # TODO: set real IP
    qdevice:
      hosts:
        pi-qdevice:
          ansible_host: 192.168.1.13   # TODO: set real IP

  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/homelab_ansible
    ansible_python_interpreter: /usr/bin/python3
